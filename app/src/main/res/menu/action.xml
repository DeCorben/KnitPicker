<menu xmlns:android="http://schemas.android.com/apk/res/android">
	<item android:id="@+id/stitchDatabase"
		android:title="Stitch Database"
		android:onClick="stDbAction"
		android:showAsAction="always"
		android:icon="@android:drawable/ic_menu_manage"
		/>
	<item android:id="@+id/newPattern"
		android:title="New Pattern"
		android:onClick="newAction"
		android:showAsAction="always"
		android:icon="@android:drawable/ic_menu_add"
		/>
	<item android:id="@+id/editPattern"
		android:title="Edit Pattern"
		android:onClick="editAction"
		android:showAsAction="always"
		android:icon="@android:drawable/ic_menu_edit"
		/>
</menu>
